import React from "react";
import styled from "styled-components";

const Layout = styled.div`
  border: 3px solid red;
  width: 758px;
  height: 158px;
`;

function ActiveBadge() {
  return <Layout>ActiveBadge</Layout>;
}

export default ActiveBadge;
